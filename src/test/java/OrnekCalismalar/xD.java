package OrnekCalismalar;

import org.junit.Assert;
import org.junit.Test;
import org.openqa.selenium.By;
import org.openqa.selenium.Keys;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.locators.RelativeLocator;
import org.openqa.selenium.support.ui.ISelect;
import org.openqa.selenium.support.ui.Select;
import utilities.TestBaseClass;

public class xD extends TestBaseClass
{
//Test02
    @Test
            public void test01(){
        //1- dropdown menuden elektronik bölümü seçin

     /*
        select.selectByValue("10");
        //2- arama kutusuna iphone yazip aratin ve bulunan sonuç sayısını yazdırın
        WebElement aramakutusu=driver.findElement(By.id("twotabsearchtextbox"));
        aramakutusu.sendKeys("iphone" + Keys.ENTER);
        //3- sonuc sayisi bildiren yazinin iphone icerdigini test edin
        String expected= "iphone";
        String actual=driver.findElement(By.xpath("//*[text()='1-24 of over 7,000 results for']")).getText();
        Assert.assertTrue(actual.contains(expected));
        //4- ikinci ürüne relative locater kullanarak tıklayin
        WebElement ilkLocat=driver.findElement(By.xpath("//div[@class='a-row a-badge-region']"));
        WebElement  ikinciÜrün=driver.findElement(RelativeLocator.with(By.tagName("img")).below(ilkLocat));
        ikinciÜrün.click();
        //5- ürünün title'ni ve fiyatını variable’a assign edip ürünü sepete ekleyelim
        String ürünTitle=driver.getTitle();
        String ürünFiat=driver.findElement(By.xpath("//div[@class='a-section a-spacing-micro']")).getText();
        driver.findElement(By.xpath("//span[@id='submit.buy-now']")).click();

      */

    }


}
